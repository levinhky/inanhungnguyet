/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "inanhungnguyet.vercel.app",
      "images.unsplash.com"
    ],
  },
};

module.exports = nextConfig;
